divident = int (input('enter divident: '))
divisor = int (input('enter divisor: '))
def divison (divident,divisor):
    print(f"Quotient : {divident / divisor} , Remainder: {divident % divisor}")
divison(divident,divisor)    